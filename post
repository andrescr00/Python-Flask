import requests
import json


url = 'http://httpbin.org/post'
args = {'nombre': 'andres','apellido':'castano','segundo_n':'fel'}

response = requests.post(url, params=args)
print(response.url)

if response.status_code == 200:
	print(response.text)
